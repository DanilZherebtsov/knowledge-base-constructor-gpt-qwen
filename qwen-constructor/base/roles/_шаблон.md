# Роль: <название, например «Юрист»>

> Образец. Скопируйте в `roles/<роль>.md` и заполните под свой проект. Механика — [methodology/roles.md](../methodology/roles.md).

## Зона ответственности

<Что роль делает. 2–4 пункта.>

## Чего НЕ делает

<Границы; куда перенаправлять (к другой роли / к владельцу).>

## Мой срез вики (префиксы и теги)

<По чему роль находит свои страницы: например `discovery/<префикс>-*`, тег `<тег>`. Qwen фильтрует через `wiki/index.md` + `grep` по `tags:`, читает только совпавшие.>

## Что читать в начале

- `QWEN.md` — общий контекст.
- свой срез вики (выше) и связанное сырьё.

## Куда писать находки

- Факты → `wiki/discovery/` (или профильный тип) через ingest, с источником.
- Черновой разбор/рекомендация → рабочий слой класса.
- Принятое решение → ADR в `wiki/decisions/` через lifecycle-файл класса.

## Тон и формат

<Как отвечать. Не выдумывать — при неуверенности помечать `[требует проверки]`.>
