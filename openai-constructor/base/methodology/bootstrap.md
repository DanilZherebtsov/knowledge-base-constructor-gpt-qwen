# Bootstrap — инициализация под конкретный проект

Запускается при первом ingest в проект без структуры или по просьбе («инициализируй структуру», «разверни вики»). **Никогда** из `query`/`lint`. Один раз; повторно — только восстановление.

Bootstrap — не штамповка фиксированных папок, а **короткая настройка под проект**. Шаблон уже содержит `AGENTS.md`, `HELP.md`, `STATE.md`, `methodology/`, `roles/`, `.codex/` (машинерия среды: `SessionStart`-хук форсинга стартовых проверок, служебная строка о согласии и оценщик готового ответа); bootstrap достраивает рабочие слои.

## Шаг 1 — короткое интервью (2–4 вопроса, дождаться ответов)

<<СЛОТ INTERVIEW-Q: вопросы интервью класса (дефолт ниже)>>

- Что за проект и в чём сейчас цель?
- Доменные конвенции (единицы, формат цитат — слот S8), если есть.

**Всегда (любой класс, вне слота выше) — спросить про валюту:** «В какой валюте деньги проекта и в каком формате писать суммы?» **Дефолта нет** — не предполагать валюту по языку или стране; ответ фиксируется в слоте S8 «Документы и нейминг» `AGENTS.md` (пример формата — с разделителем тысяч: `220 000 ₽`, `$1,200`, `€990`). Проект без денежных сумм — так и записать («суммы не используются»). Пропустить вопрос, если S8 уже заполнен.

**Всегда (любой класс, вне слота выше) — спросить про код:** «Будет ли в проекте собственный код — сайт, лендинг, бот, приложение, скрипты?»

- **Да** → подключить работу с кодом (механика `software-engineering`: код-папка в корне, цикл исполнения, дисциплина владения, для веб-продукта — пара ролей «правит / деплоит»).
- **Нет** → ничего не подключаем; способность можно **создать позже в любой момент** — фразой «будем писать код» или автоматически, когда код впервые придёт (привратник в [ingest.md](ingest.md) предложит сам). Откуда берутся файлы способности и что делать, если их не достать, — [lint.md](lint.md), «Подключить способность, которой в проекте нет».
- Пропустить вопрос, если механика `software-engineering` уже подключена.

## Шаг 2 — создать (чего нет, не перезаписывая существующее)

- `input/.gitkeep` — свалка для входящих (человек кидает сюда любые материалы; Codex раскладывает по `raw/` на ingest, человеку подпапки знать не нужно).
- `raw/.gitkeep` — пустой; подпапки заранее не заводятся (см. «Что НЕ создаётся»).
- `wiki/` + type-папки класса (<<СЛОТ S2>>, каждая с `.gitkeep`); `wiki/index.md` (`# Wiki Index`); `wiki/log.md` (`# Wiki Log` + `## [YYYY-MM-DD] bootstrap | инициализация`).
- `output/.gitkeep` — **корень рабочих файлов, есть у всех классов**; создаётся пустым, подпапки (`drafts/`, `черновики/`, папки под живые документы) — по факту, не заранее. <<СЛОТ S4: дополнительные слои для классов с кодом — `specs/` + `src/` + `data/`>>

## Шаг 3 — заполнить сопутствующие файлы

- «О проекте» в `AGENTS.md` из ответов интервью. После заполнения **удалить инструктивную сноску** — блок `>` под заголовком «О проекте» (он для составителя, не для проекта).
- Стартовый раздел `STATE.md` (профиль/стадия); `_Обновлено:_` = сегодня.

## Что НЕ создаётся при bootstrap

- **Роли** — заранее не заводим; сообщить, что любую роль можно создать в любой момент «создай роль <название>» (см. [roles.md](roles.md)), и что по желанию можно заранее закинуть в `input/` описание роли и материалы для её работы — тогда она создастся сразу квалифицированно.
- **Код-папка** — пустой не заводится. Подключили работу с кодом (вопрос про код в Шаге 1) — папка появляется, когда код реально приходит или создаётся, по решению человека. Не подключили — можно добавить способность позже в любой момент ([lint.md](lint.md), «Подключить способность, которой в проекте нет»).
- **`BRAND.md`** — сам не заводится вообще. Появляется только **по согласию человека**: после того как он принял первую визуальную работу, Codex один раз предлагает сохранить согласованный стиль на будущее. Отказ — нормальный исход, файла просто нет. Пришёл готовый фирстиль от клиента — файл заводится сразу при его разборе ([ingest.md](ingest.md)): согласие уже дано этим действием. Пишется всегда **из построенного**, а не заранее.
- **`tmp/`** — расходный слой длинного прохода (журнал прогресса, логи, промежуточные куски); пустым не создаётся, появляется при первом длинном проходе. Под git — добавить `tmp/` в `.gitignore` в момент создания, до первой записи внутрь.
- **`archive/`** — устаревшее из `output/` и `tmp/`, что не стоит удалять; пустым не создаётся, появляется при первом выпуске ([lint.md](lint.md)). Под git — добавить `archive/` в `.gitignore` в момент создания, до переноса первого файла.
- **Подпапки `raw/`** — заранее не заводятся и на интервью не выясняются: пустая папка под источник, которого ещё нет, — угадывание. Первый материал раскладывается на ingest, тогда же появляется подпапка и вписывается в дерево «Архитектура» `AGENTS.md` ([ingest.md](ingest.md), шаг 2). Человеку подпапки знать не нужно — он кидает в `input/`.
- Папки сверх минимума, **включая подпапки рабочего слоя** (`drafts/`, `черновики/`), — по смыслу, когда появится потребность (правило «Структура растёт по мере надобности» в [AGENTS.md](../AGENTS.md)).

После bootstrap — если под git — коммит `bootstrap: инициализация структуры`; без git пропускается.
