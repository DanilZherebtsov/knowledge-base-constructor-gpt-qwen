# AGENTS.md — конструктор шаблонов (проект ещё не собран)

Этот проект содержит **конструктор LLM-вики-шаблонов**, но рабочая структура ещё не собрана. При первом контакте твоя задача — не отвечать как в обычном чате, а **проактивно запустить настройку проекта** через короткое интервью.

## Триггер настройки (ALWAYS-ON, пока проект не собран)

**Признак «ещё не собрано»:** в корне лежат `base/`, `mechanics/`, `presets/`, `START-HERE.md` (леса конструктора), но нет собранного проекта (`wiki/`, `STATE.md`, классового `methodology/`).

Пока признак держится, предложить настройку — твоя первая задача **в любой реплике и в любом чате**, а не только в первой:

1. Поздоровайся и в одну фразу скажи, что это конструктор и сейчас быстро поставим проект на рельсы.
2. **Начни интервью по [START-HERE.md](START-HERE.md)** — первый вопрос: «Чем будешь заниматься в этом проекте?».

Не жди, пока человек сам заговорит о задаче: каким бы ни было его сообщение — «привет», офтоп, готовое описание проекта — веди к настройке (описал проект сразу — тем лучше, переходи к делу).

**Если первая реплика уже ушла другой** — приветствием, ответом на офтоп, предложением «посмотреть, что в папке» (в том числе сгенерированной средой до того, как ты прочитал этот файл), — предложи настройку **в следующей же реплике**, не дожидаясь просьбы. Первый контакт не истекает: он длится, пока проект не собран. Человек уже в процессе интервью — продолжай его, не начинай заново.

**Открыта не та папка.** Репозиторий несёт две редакции — `openai-constructor/` и `qwen-constructor/`. Если ты видишь их обе, значит открыт корень репозитория, а не редакция: скажи об этом человеку и попроси открыть нужную папку как проект. Собирать из корня нельзя.

## Карта конструктора

- **[START-HERE.md](START-HERE.md)** — интервью и выбор механик (фронт-дверь).
- **[ASSEMBLY.md](ASSEMBLY.md)** — механика сборки проекта из `base/` + манифеста + механик.
- **[mechanics-catalog.md](mechanics-catalog.md)** — реестр отстёгиваемых механик (роли, claim-граф, lifecycle).
- `base/` — скелет; `presets/` — манифесты классов; `mechanics/` — drop-in механики.

## После сборки

Собранный проект получает **свой** корневой `AGENTS.md` (из `base/`, заполненный) — он замещает этот файл-лаунчер. Леса конструктора — всё, что пришло вместе с ним, включая этот файл, — удаляются; точный перечень и порядок — [START-HERE.md](START-HERE.md), шаг 5.
