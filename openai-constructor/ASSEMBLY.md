# Сборка проекта из конструктора

Эту процедуру выполняет Codex в **чистой комнате**: видит ТОЛЬКО эту папку (`base/`, `mechanics/`, `presets/`, каталог). Папку `templates/` НЕ читает — иначе тест ложно пройдёт (дозаполнит пробелы по памяти).

## Вход

Обычный вход — разговорный, через [START-HERE.md](START-HERE.md): интервью без обязательного называния класса определяет стартовый пресет и доп-механики. Этот файл (ASSEMBLY) — механика самой сборки, на которую START-HERE опирается (шаг 4). Прямой вход «имя пресета» (`saas-product` / …) — режим детерминированной сборки/теста.

## Процедура

> **Язык разговора — сквозной.** Всё, что видит человек в ходе сборки, включая служебные реплики о ходе работ, — на его языке и без внутреннего жаргона. Правило и стоп-слова — [START-HERE.md](START-HERE.md), «Язык разговора»; шаг 9 ниже — частный случай, а не единственное место, где запрет действует (ADR-0030).

1. **Старт от base.** Проект уже собран (в строке 3 корневого `AGENTS.md` отпечаток «**Сборка:**», слотов нет) — не собирать: см. [START-HERE.md](START-HERE.md), «Проект уже собран». В корне уже есть `wiki/` или `STATE.md`, а собранного `AGENTS.md` нет — сборка прервалась: продолжить с места остановки, `base/` поверх не копировать. Иначе скопировать `base/` как заготовку проекта (скелет со слотами `<<СЛОТ ...>>`). Папку `.codex/` копировать **отдельным действием**, не в общем потоке: среда может отклонить запись в неё отдельно от остальных файлов. Отклонила — по `base/methodology/lint.md`, раздел «Когда среда не даёт»; сборку из-за этого не останавливать.
2. **Прочитать манифест** `presets/<class>.md`.
3. **Подставить скалярные слоты** из манифеста в base:
   - `title-word` → слот `TITLE` в H1 + проза вводных абзацев; `S1` «О проекте» — из интервью (шаг 7).
   - **отпечаток сборки (provenance)** → слот `PROVENANCE` в `AGENTS.md` (строка 3). Прочитать `versions.json` конструктора и записать строку из РЕАЛЬНО ВШИТЫХ частей: `> **Сборка:** <class> · base@<v> · <каждая включённая механика>@<v> · <манифест класса>@<v>. На lint Codex сверяет части с апстрим-`versions.json` — [methodology/lint.md](methodology/lint.md).` Пример (saas): `> **Сборка:** saas-product · base@33 · spec-lifecycle@6 · software-engineering@7 · design@1 · saas-product@4.` Механика `design` включена во всех пресетах, поэтому в отпечатке присутствует всегда (ADR-0038). Роли в отпечаток НЕ пишутся — это базовая машинерия, не механика (ADR-0027). Не подключённые опциональные механики НЕ пишутся. Это единственное, что нужно проекту для обновлений; CHANGELOG/полную историю проект не несёт.
   - `central-type` (S2) → дерево `wiki/` + описание типа; `S3` → `page-conventions.md`; `work-layers` (S4) → дерево; `state-sections` (S5) → `STATE.md` + `state-rules.md`; `authority` (S7); `domain-conv` (S8); `INTERVIEW-Q` → `bootstrap.md`; `raw-defaults` → `ingest.md` (словарь имён подпапок `raw/`; папки при сборке не создаются); `domain-lint` → `lint.md`; `close-op` → `index-log-format.md`; `KNOWLEDGE-UNIT` → `ingest.md`; `HELP-OPS` (в `HELP.md`) → перечень реально вшитых операций/потоков, принцип «только собранное» (те же пункты, что онбординг в шаге 9, человеческим языком).
   - Слоты с пометкой «иначе пусто», которые не заполняет ни одна подключённая механика (`DEADLINE-CHECK` у класса без `decision-lifecycle`) → удалить маркер (резолв в пусто).
4. **Вшить механики** из `manifest.mechanics` по их `mechanics/<имя>/_about.md` (раздел «Пошаговый wiring») + обязательные зависимости (`claim-graph` для `question-lifecycle`).
   - **Конфликт за центральный тип разрешать самому, молча** (ADR-0030). Претендуют двое (`claim-graph` / `spec-lifecycle` / `decision-lifecycle`) — победитель определяется **критерием** в [mechanics-catalog.md](mechanics-catalog.md) («Критерий центрального типа»): пересматривает ли проект собственные выводы (→ `claim-graph`) или действует и идёт дальше (→ lifecycle класса). Проигравший не подключается. **Человеку конфликт не выносить** и внутренними словами не описывать — у него нет базы для такого выбора. Наружу — только последствие на его языке, и только если оно у него есть.
5. **Базовая машинерия не деактивируется.** Роли (`roles.md`, `roles/_шаблон.md`, раздел «Роли», строка bootstrap) присутствуют всегда — из base (ADR-0027). Заполнить слот `ROLES-FILL` примерами ролей класса. Опциональных механик, требующих выкорчёвывания из base-скелета, сейчас нет.
6. **Состав выхода — что идёт в проект.** В собранный проект идут ТОЛЬКО: `AGENTS.md`, `HELP.md` (памятка человеку, слот `HELP-OPS` заполнен), `STATE.md`, `methodology/*.md` (классовый набор), `roles/` (всегда — базовая машинерия ролей), **`.codex/`** (машинерия среды из base: `hooks.json` + `config.toml` + `hooks/` — `freshness_check.py` (SessionStart) и `consent_nudge.py` (UserPromptSubmit); копируется как есть, слотов не несёт), рабочие слои по `work-layers` (`specs/` и т.д., с учётом ленивого создания — `spec-lifecycle`). **НЕ идут:** `base/README.md`, корневые `README.md`, `CHANGELOG.md` (единый, в корне конструктора), `versions.json` и никакие внутренние файлы конструктора (`ASSEMBLY.md`, `EXPERIMENT.md`, `mechanics-catalog.md`, `mechanics/`, `presets/`). **CHANGELOG — артефакт версионирования репозитория шаблонов** (его ведёт операция «пересобрать пресеты / распространить base», фиксируя, что изменилось), а НЕ продукт пер-проектной сборки. Проект несёт только строку-метку версии (шаг 3).
7. **Интервью с пользователем** (bootstrap шаг 1): instance-данные — «О проекте» (S1), доменные конвенции, валюта, код. Про будущие источники не спрашивать — `raw/` стартует пустой, подпапки заводит ingest по приходу материала. Заполнить, удалить инструктивные сноски и base-врезку «Это base-template — скелет».
8. **Гигиена.** `grep -rn '<<СЛОТ' .` и `grep -rn 'ROLES-FILL' .` — пусто (литералы `<<СЛОТ …>>` внутри base-прозы `lint.md` — не маркеры, а текст инструкции). Нет битых ссылок на не подключённые опциональные механики.
9. **Онбординг (финальный хэндофф).** После сборки и попытки уборки лесов — короткое сообщение на языке человека, без внутреннего жаргона (никаких «ingest / query / lint / механики / центральный тип / слои / версии»). **Операции построчно НЕ перечисляй** — детальный перечень уже в `HELP.md` (слот `HELP-OPS`, заполнен на этой сборке; принцип «только собранное» соблюдён там). Дай общую ориентацию тремя штрихами и отправь за деталями к справке:
   - Первая фраза — по факту уборки: «Проект собран, временные файлы конструктора убраны. Как пользоваться:» / «…временные файлы сложены в папку `_to_delete` — перетащи её в Корзину. Как пользоваться:» / «…временные файлы ещё лежат — уберу, как разрешишь. Как пользоваться:». Просьба переключить режим и вопрос «оставить или сложить» (`methodology/lint.md`, «Удаление, когда среда не даёт», пп. 3–4) и вопрос о записи хуков (там же, «Запись хуков, когда автоматический режим не даёт») идут отдельными сообщениями раньше хэндоффа и заканчивают ход; хэндофф — по ответу. Ответ не о переключении и не о судьбе файлов — вариант «ещё лежат» и дальше по делу.
   - **Материалы** — «Новые материалы — тикеты, заметки с созвонов, письма, changelog'и, что угодно — кидай в папку `input/`, потом скажи „обработай“. Я разложу и усвою в память проекта.»
   - **Новый чат под задачу** — «Каждую новую задачу лучше начинать в новом чате. Хранить чаты не нужно — всё сохранённое лежит в памяти проекта на диске (`wiki/`) и видно из любого чата; важное по ходу я предложу записать — решаешь ты.» Разрешение на удаление в этом чате выдано — не «лучше», а указанием и с причиной: «Первую задачу начни в новом чате: в этом у меня осталось разрешение удалять файлы в папке».
   - **Справка** — «В любой момент напиши `help` — покажу подробно, что я умею в этом проекте.» (Слово объявляем **без слэша**: слэш-команды перехватывает сам клиент, и до правил проекта они не доходят. Прочие синонимы — `guide`, `справка`, `помощь` и т.д. — продолжают работать, но человеку называем одно слово.)
   Заметки (стадия, ещё не созданные слои, git) в хэндофф НЕ выносим — стало короче сознательно; текущее состояние человек берёт из `STATE.md`, полный перечень возможностей — по `help` из `HELP.md`.

## Результат

Содержимое проекта (`AGENTS.md` + `methodology/` + `STATE.md` + рабочие слои), функционально эквивалентное `templates/<class>/`. Совпадать побайтово не обязано — косметический пересказ base-прозы роняем сознательно; обязана присутствовать каждая реальная доработка из `customizations_checklist` манифеста. `CHANGELOG.md`/`README.md` — вне сборки (репо-/версионный слой).
